# Problems Submission
---

## q6_CopyBinaryTree

**Problem Type:** Tree / Recursion  

**Description:**  
Creates a deep copy of a binary tree by recursively copying each node and preserving the original tree structure.

**Time Spent:** —  

**Complexity Analysis:**  
- **Time Complexity:** O(n), where *n* is the number of nodes in the tree  
- **Space Complexity:** O(h), where *h* is the height of the tree (recursion stack)  

---

## q7_ValidateBST

**Problem Type:** Tree / Recursion  

**Description:**  
Determines whether a binary tree is a valid Binary Search Tree (BST) by ensuring each node’s value falls within valid min and max bounds.

**Time Spent:** 20 minutes  

**Complexity Analysis:**  
- **Time Complexity:** O(n), where *n* is the number of nodes  
- **Space Complexity:** O(h), where *h* is the height of the tree  

---

## q8_DedupSortedLinkedList

**Problem Type:** Linked List  

**Description:**  
Removes duplicate values from a sorted singly linked list by skipping over consecutive nodes with the same value.

**Time Spent:** 15 minutes  

**Complexity Analysis:**  
- **Time Complexity:** O(n), where *n* is the number of nodes  
- **Space Complexity:** O(1)  

---

## q9_MoveNodeToFront

**Problem Type:** Linked List  

**Description:**  
Moves the k-th node from the end of a singly linked list to the front while preserving the order of the remaining nodes.

**Time Spent:** 25 minutes  

**Complexity Analysis:**  
- **Time Complexity:** O(n), where *n* is the number of nodes  
- **Space Complexity:** O(1)  

---

## q10_PalindromeDoublyLinkedList

**Problem Type:** Linked List / Two Pointers  

**Description:**  
Checks whether a doubly linked list is a palindrome by comparing values from the front and back simultaneously.

**Time Spent:** 20 minutes  

**Complexity Analysis:**  
- **Time Complexity:** O(n), where *n* is the number of nodes  
- **Space Complexity:** O(1)  

---

## q11_DisconnectCycle

**Problem Type:** Linked List / Hash Set  

**Description:**  
Detects and removes a cycle in a linked list by tracking visited nodes using a set and breaking the link when a cycle is found.

**Time Spent:** 20 minutes  

**Complexity Analysis:**  
- **Time Complexity:** O(n), where *n* is the number of nodes  
- **Space Complexity:** O(n)  

---

## q12_LeftViewBinaryTree

**Problem Type:** Tree / Breadth-First Search (Level Order)  

**Description:**  
Returns the left view of a binary tree by performing a level-order traversal and selecting the first node encountered at each level.

**Time Spent:** 15 minutes  

**Complexity Analysis:**  
- **Time Complexity:** O(n), where *n* is the number of nodes in the tree  
- **Space Complexity:** O(n), due to queue storage for level-order traversal  

---

## q13_FloorInBST

**Problem Type:** Binary Search Tree / Recursion  

**Description:**  
Finds the floor of a given target value in a Binary Search Tree. The floor is defined as the greatest value in the BST that is less than or equal to the target.

**Time Spent:** 15 minutes  

**Complexity Analysis:**  
- **Time Complexity:** O(h), where *h* is the height of the BST  
- **Space Complexity:** O(h), due to recursion stack depth  

---

